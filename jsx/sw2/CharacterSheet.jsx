'use strict';

module.exports = React.createClass({
    render: function () {
        return (
            <div class="sw2-character-sheet">
                <header>
                    <h1>{this.props.data.name}</h1>
                </header>
                name
            </div>
        );
    }
});
